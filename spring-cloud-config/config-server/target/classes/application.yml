server:
  port: 7071
spring:
  application:
    name: config-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jul1en-Lin/Spring-Cloud-Config.git
          default-label: main #配置文件分支
          search-paths: config-center  #配置文件所在根目录